{{- if or (eq .machine_type "work") (eq .machine_type "hybrid") -}}
[nowtech]
aws_access_key_id = {{ (bitwardenFields "item" (print "AWS " .work_email))."Access key" }}
aws_secret_access_key = {{ (bitwardenFields "item" (print "AWS " .work_email))."Secret access key" }}
{{ end -}}
{{ "" }}
